{% extends "base.html.j2" %}

{% block content %}
<div class="row">
  <div class="col-md-12">
    <h2 class="text-center">Edit Post</h2>
  </div>
</div>
<div class="row">
    <div class="col-xs-12 col-sm-8 offset-sm-2">
      <form class="form-editpost" method="post">
        <div class="form-group">
          <label for="subjectInput">Subject</label>
          <input class="form-control" id="subjectInput" type="text" value="{{subject}}" name="subject" placeholder="Witty title here..." required>
          <input type="hidden" name="post_id" value="{{post.key().id()}}">
        </div>
        <div class="form-group">
          <label for="contentInput">Content</label>
          <textarea class="form-control post-content" id="contentInput" name="content"placeholder="Your wise words here" required>{{content}}</textarea>
        </div>
        {% if error %}
          <div class="alert alert-danger"><strong>Woops! </strong>{{error}}</div>
        {% endif %}
        <div class="text-center">
          <button type="submit" class="btn btn-primary">Submit</button>
          <a class="btn btn-primary" href="/blog/{{post.key().id()}}">Cancel</a>
        </div>
      </form>
    </div>
</div>
  <!-- <form class="editpost" method="post">
    <label>
      <h3>Subject</h3>
      <input type="text" name="subject" value="{{subject}}">
      <input type="hidden" name="post_id" value="{{post.key().id()}}">
    </label>
    <label>
      <h3>blog</h3>
      <textarea name="content">{{content}}</textarea>
    </label>
    <div class="error">
      {{error}}
    </div>
    <input type="submit">
  </form>
  <div >
    <a href="/blog/{{post.key().id()}}">Cancel</a>
  </div> -->
{% endblock %}
